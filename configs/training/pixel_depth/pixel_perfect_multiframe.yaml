enabled: true
semantic_input_dim: 2048
refiner:
  hidden_dim: 128
  depth: 4
  num_heads: 4
  coarse_patch_size: 16
  fine_patch_size: 8
temporal:
  enabled: true
  hidden_dim: 128
  num_heads: 4
  depth: 2
  reference_mode: random_valid
  preserve_frame_order: true
geometry:
  enabled: true
  occlusion_mask: true
  dynamic_mask: true
  invalid_mask: true
  max_depth_m: 1.2
  relative_depth_tolerance: 0.03
flow:
  log_residual_scale: 1.0
  gradient_loss_weight: 0.2
  objective_weight: 1.0
  edge_objective_weight: 0.5
  multiview_objective_weight: 0.5
  time_sampling: logit_normal
  ode_steps: 4
  seed_offset: 10000
  residual_gate_initial: 1.0
optimization:
  train_base_heads: true
